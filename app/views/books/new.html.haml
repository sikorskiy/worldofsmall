- content_for :bigtext, "Создание новой книги"
- content_for :smalltext, "Заполняйте все поля аккуратно"
.container
  .row
    - if logged_in?
      .col-md-8
        = simple_form_for @book, defaults: { input_html: {class: "form-control-sm"}} do |f|
          = f.input :name, label: "Название книги"
          = f.input :author, label: "Автор"
          = f.input :illustrator, label: "Иллюстратор"
          = f.input :translator, label: "Переводчик"
          = f.input :publishing_house, label: "Издательство"
          = f.input :creation_year, label: "Год написания"
          = f.input :info, label: "Описание", input_html: {rows: "5"}
          = f.input :image, as: :file
          = f.button :submit, "Сохранить книгу", class: "button btn-success"
    - else
      %p
        Только зарегистрированные пользователи могут создавать новые книги



