- provide :title, "Редактирование книги"
- content_for :bigtext, "Редактирование книги"
- content_for :smalltext, "Все поля важны, заполняйте их максимально внимательно"
.container
  .row
    - if logged_in?
      .col-md-8
        = simple_form_for @book, defaults: { input_html: {class: "input-block-level"}} do |f|
          = f.input :name, label: "Название книги"
          = f.input :author, label: "Автор"
          = f.input :illustrator, label: "Иллюстратор"
          = f.input :translator, label: "Переводчик"
          = f.input :publishing_house, label: "Издательство"
          = f.input :creation_year, label: "Год написания"
          = f.input :info, label: "Описание", input_html: {rows: "5"}
          = f.input :image, as: :file
          - if current_user.is_admin?
            = f.input :start_age, label: "С", placeholder: "С какого возраста можно начинать читать"
            = f.input :finish_age, label: "По", placeholder: "До какого возраста стоит читать"
          = f.button :submit, "Редактировать книгу", class: "button btn-success"
    - else
      %p
        Только зарегистрированные пользователи могут редактировать книги



